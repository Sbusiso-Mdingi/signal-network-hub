# Runner validation: GitHub-owned actions only
name: Publish current React site

on:
  push:
    branches: [main]
    paths:
      - "src/**"
      - "public/**"
      - "CNAME"
      - "vite.config.ts"
      - "package.json"
      - "bun.lock"
      - "scripts/publish-react-site.mjs"
      - "scripts/enforce-public-domain.mjs"
      - ".github/workflows/publish-current-react-pages.yml"
  pull_request:
    branches: [main]
    paths:
      - "src/**"
      - "public/**"
      - "CNAME"
      - "vite.config.ts"
      - "package.json"
      - "bun.lock"
      - "scripts/publish-react-site.mjs"
      - "scripts/enforce-public-domain.mjs"
      - ".github/workflows/publish-current-react-pages.yml"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: publish-current-react-site-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22.16.0"

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Build current React routes into docs
        run: npm run build:static

      - name: Commit generated Pages output
        if: github.event_name == 'push'
        run: |
          if git diff --quiet -- docs; then
            echo "docs/ already matches the current React build."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A docs
          git commit -m "build: publish current React site to Pages"
          git push origin HEAD:main
